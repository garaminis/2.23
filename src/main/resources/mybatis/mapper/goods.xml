<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.study.shopping.GoodsDAO">
<!-- <select id="add" resultType="com.study.shopping.GoodsDTO">
	SELECT userid, passwd FROM member
</select> -->
<insert id="goodsAdd">
	INSERT INTO goods0 SET category = #{param1}, title = #{param2}, name = #{param3}, price = #{param4}, stock = #{param5}, img1 = #{param6}, content = #{param7}
</insert>
<select id="itemList" resultType="com.study.shopping.GoodsDTO">
	SELECT b.name name, a.id id, img1, title, price FROM goods0 a, goodscategory b WHERE a.category = #{param1} and b.id = #{param1} limit #{param2}, #{param3};
</select>
<select id="itemInfo" resultType="com.study.shopping.GoodsDTO">
	SELECT a.*, b.pay pay FROM goods0 a, delivery b WHERE a.id = #{param1} and a.delpay = b.id
</select>
</mapper>